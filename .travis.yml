os:
  - linux
  - osx
language: rust
rust:
  - stable
  - beta
  - nightly
before_script:
  - rustup component add clippy
  - rustup component add rustfmt
script:
  - cargo test
  - cargo ci-clippy
  - cargo ci-fmt
jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true
